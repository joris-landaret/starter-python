

def mysort(list):
    mylist=[]
    
    for param in params:
        if isinstance(param,(int)):
            mylist.append(param)
        else:
            print ("Atention")
    
    permutation = True:
    passage = passage + 1

    for en_cour in range(0, len(mylist) - passage):
        if mylist[en_cour] > mylist[en_cour + 1]:
            permutation = True
            #on echange les 2 elements
            mylist[en_cour], mylist[en_cour + 1]
            mylist[en_cour + 1]  mylist[en_cour]